generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model user {
  id           Int    @id @default(autoincrement())
  name         String @unique
  phoneNumber  String
  companyName  String
  designation  String
  token        String @unique
}